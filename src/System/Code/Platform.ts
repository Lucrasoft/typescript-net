module System {
	class Platform {

		//static extern uname: number(buf: IntPtr);

		//static checkedIsMacOS: boolean;
		//static isMacOS: boolean;

		//public static bool IsMacOS {
		//	get {
        //if (Environment.OSVersion.Platform != PlatformID.Unix)
        //    return false;

        //if (!checkedIsMacOS) {
		//			IntPtr buf = Marshal.AllocHGlobal(8192);
        //    if (uname(buf) == 0) {
		//				string os = Marshal.PtrToStringAnsi(buf);
        //        if (os == "Darwin")
        //            isMacOS = true;
        //    }
        //    Marshal.FreeHGlobal(buf);
        //    checkedIsMacOS = true;
        //}

        //return isMacOS;
        //    }
		//}
    }
}
 module System.Net {
	export interface ICredentialsByHost
	{
//todo		NetworkCredential GetCredential (string host, int port, string authenticationType);
    }

     System.Type.registerInterface("System.Net.ICredentialsByHost");
}